Options -Indexes
RewriteEngine On

# Allow direct access to real files/dirs (CSS/JS/images/uploads)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else through front controller
RewriteRule ^ index.php [QSA,L]
